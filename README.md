# FormLens
FormLens — bring your own Anthropic API key and extract structured data from any PDF form. Schema discovery, iterative refinement, bulk extraction, and persistent sessions. No backend required.
